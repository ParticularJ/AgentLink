# 核心依赖
pandas>=2.0.0
numpy>=1.24.0

# 数据源（至少安装一个）
akshare>=1.10.0      # 推荐，免费，数据丰富
tushare>=1.2.0       # 需要注册token（pro版需要付费）
baostock>=0.8.8      # 免费，数据较稳定
yfinance>=0.2.0      # 有限支持A股，适合海外市场

# 可选依赖
matplotlib>=3.7.0    # 用于回测图表
